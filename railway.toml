[build]
builder = "nixpacks"

[deploy]
startCommand = "cd greens-acc-platform/backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
